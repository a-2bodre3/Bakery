import { ProductAnimation } from './product-animation';

describe('ProductAnimation', () => {
  it('should create an instance', () => {
    const directive = new ProductAnimation();
    expect(directive).toBeTruthy();
  });
});
